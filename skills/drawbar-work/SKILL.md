---
name: drawbar-work
description: Implement the next drawbar Linear story test-first, review it, open a pull request, and retain lessons in the project knowledge base. Use when the user asks to work on a drawbar story or parent issue.
---

# Work with drawbar

Follow the authoritative workflow in `commands/drawbar-work.md` in this plugin. Treat the requested issue id as that workflow's `$ARGUMENTS`. Preserve the workflow's human-review and Linear-status boundaries. Use connected Linear MCP tools when available.
