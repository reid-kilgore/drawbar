---
name: drawbar-plan
description: Decompose a locked Linear design into ordered, testable story sub-issues. Use when the user asks to plan a drawbar issue.
---

# Plan with drawbar

Follow the authoritative workflow in `commands/drawbar-plan.md` in this plugin. Treat the requested parent issue id and optional `--project` value as that workflow's `$ARGUMENTS`. Use connected Linear MCP tools when available.
